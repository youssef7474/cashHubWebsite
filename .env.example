NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Canonical site URL used for SEO (sitemap, Open Graph, JSON-LD)
NEXT_PUBLIC_SITE_URL=https://cashhub.app

# Website booking uses SQL functions in supabase/website-booking.sql
# Run that file once in the Supabase SQL Editor (no service-role key needed).
